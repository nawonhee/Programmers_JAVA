class Solution {
    public double solution(int[] numbers) {
        double answer = 0;
        double sum = 0;
        double n_length = numbers.length;
        
        
        for(int i=0;i<n_length;i++){
            sum+=numbers[i];
        }
        
        answer = sum/n_length;
        
        return answer;
    }
}
